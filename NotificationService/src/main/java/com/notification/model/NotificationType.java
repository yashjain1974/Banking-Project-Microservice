package com.notification.model;

public enum NotificationType {
	 EMAIL,
     SMS,
     IN_APP, // Added based on Channel Management in source
     PUSH    // Added based on Channel Management in source
}
